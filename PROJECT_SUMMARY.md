# Итоговая документация проекта

## 📋 Что было сделано

Создан комплексный ИИ-агент, который объединяет два независимых проекта:

1. **ClickHouse агент** - для работы с базой данных ClickHouse
2. **Julius агент** - для анализа данных и построения графиков на Python

## 🎯 Результат

Получился единый агент, который:
- Принимает запрос пользователя на естественном языке (русский)
- Обращается к ClickHouse и выгружает нужные данные в Parquet
- Анализирует данные с помощью Python (pandas, numpy)
- Строит графики (matplotlib, seaborn)
- Сохраняет историю диалога в SQLite
- Возвращает результат в удобном формате (текст + графики)

## 📁 Структура проекта

### Основные файлы

| Файл | Описание |
|------|----------|
| `config.py` | Конфигурация из .env файла |
| `clickhouse_client.py` | Клиент ClickHouse с экспортом в Parquet |
| `python_sandbox.py` | Выполнение Python кода с захватом графиков |
| `chat_storage.py` | SQLite хранилище истории диалогов |
| `tools.py` | Определения инструментов для Claude |
| `composite_agent.py` | **Главный агент** - агентный цикл tool_use |
| `test_agent.py` | Тестовый файл для запуска агента |
| `api_server.py` | FastAPI сервер (опционально) |

### Документация

| Файл | Описание |
|------|----------|
| `README.md` | Общая информация о проекте |
| `INSTALLATION.md` | Подробная инструкция по установке |
| `QUICKSTART.md` | Быстрый старт за 5 минут |
| `FULL_IMPLEMENTATION_SPEC.md` | Полная спецификация реализации |

### Вспомогательные файлы

| Файл | Описание |
|------|----------|
| `setup.sh` | Скрипт автоматической установки |
| `requirements.txt` | Python зависимости |
| `.env.example` | Шаблон конфигурации |
| `.gitignore` | Игнорируемые файлы |
| `agent_service.service` | Systemd сервис для автозапуска |

## 🚀 Как использовать

### Вариант 1: Тестовый запуск (рекомендуется для начала)

```bash
# 1. Установка
bash setup.sh

# 2. Настройка .env
nano .env

# 3. Запуск тестового агента
source venv/bin/activate
python test_agent.py
```

### Вариант 2: API сервер (для интеграции с веб-интерфейсом)

```bash
# Запуск API сервера
python api_server.py

# API будет доступен на http://localhost:8000
# Документация: http://localhost:8000/docs
```

### Вариант 3: Использование из Python кода

```python
from composite_agent import CompositeAnalysisAgent
import uuid

agent = CompositeAnalysisAgent()
session_id = str(uuid.uuid4())

result = agent.analyze(
    user_query="Покажи топ-10 товаров по выручке",
    session_id=session_id
)

print(result["text_output"])
```

## 🔧 Технические детали

### Архитектура

```
Пользователь
    ↓ (запрос на русском языке)
Claude Sonnet 4
    ↓ (решает какие tools вызвать)
Composite Agent
    ↓ (выполняет tools)
┌─────────────────────┐
│ list_tables         │ → Схема БД
│ clickhouse_query    │ → Данные в Parquet
│ python_analysis     │ → Анализ + графики
└─────────────────────┘
    ↓
Результат (текст + графики)
```

### Почему Parquet, а не CSV?

ClickHouse имеет сложные типы данных:
- `Array(String)` - массивы
- `Map(String, UInt64)` - словари
- `Tuple(Int32, String)` - кортежи

Parquet сохраняет эти типы нативно, CSV - нет.

### Механизм tool_use

Агент использует **нативный механизм tool_use** от Anthropic:

1. Claude анализирует запрос пользователя
2. Claude решает какие инструменты нужно вызвать
3. Агент выполняет инструменты
4. Claude получает результаты
5. Если нужно - Claude вызывает еще инструменты
6. Когда всё готово - Claude формирует ответ

**Автоматический retry:** Если Python код упал с ошибкой, Claude **видит ошибку** и **исправляет код** в следующей итерации.

### История диалога

- Хранится в **SQLite** (`chat_history.db`)
- **Скользящее окно**: только последние 20 сообщений
- **Автоочистка**: сессии старше 24 часов удаляются
- **Без графиков**: в историю сохраняется только текст

### Безопасность

- ✅ Только SELECT запросы к ClickHouse
- ✅ Python код выполняется в изолированном namespace
- ✅ SSL подключение к ClickHouse
- ✅ Sanitization входных данных (UTF-8)
- ✅ Автоматическое добавление LIMIT к SQL запросам

## 📊 Примеры работы

### Пример 1: Простой запрос

**Запрос:** "Покажи список таблиц"

**Что делает агент:**
1. Вызывает `list_tables`
2. Возвращает структуру БД

### Пример 2: Выгрузка данных

**Запрос:** "Выгрузи первые 100 записей из orders"

**Что делает агент:**
1. Вызывает `clickhouse_query` с SQL:
   ```sql
   SELECT * FROM orders LIMIT 100
   ```
2. Сохраняет в Parquet
3. Возвращает превью (первые 5 строк)

### Пример 3: Анализ с графиком

**Запрос:** "Покажи топ-10 товаров по выручке с bar chart"

**Что делает агент:**
1. Вызывает `clickhouse_query`:
   ```sql
   SELECT product_name, SUM(revenue) as total_revenue
   FROM orders
   GROUP BY product_name
   ORDER BY total_revenue DESC
   LIMIT 10
   ```
2. Вызывает `python_analysis`:
   ```python
   # Построение bar chart
   plt.barh(df['product_name'], df['total_revenue'])
   plt.xlabel('Выручка, ₽')
   plt.title('Топ-10 товаров по выручке')

   # Таблица
   result = df.to_markdown(index=False)
   ```
3. Возвращает график + таблицу + выводы

## 🔍 Особенности реализации

### Из CLI агента ClickHouse (test_clickhouse_python_agent-main)

✅ Прямое подключение к ClickHouse через `clickhouse-connect`
✅ Экспорт данных в Parquet
✅ Механизм tool_use с Anthropic Messages API
✅ UTF-8 sanitization для предотвращения encoding errors
✅ Автоматическое добавление LIMIT к SQL запросам
✅ Работа БЕЗ SSL сертификата (verify=False)

### Из Julius v2 (Julius_v2-main)

✅ Захват matplotlib графиков в base64
✅ Настройки matplotlib для серверного рендеринга (Agg backend)
✅ Очистка matplotlib после каждого выполнения (`plt.close('all')`)
✅ Поддержка seaborn для красивых графиков
✅ DPI=150 для чётких изображений

### Новое в комплексном агенте

✅ Объединение двух агентов в один
✅ История диалога в SQLite со скользящим окном
✅ Автоочистка временных файлов и старых сессий
✅ Убирание графиков из tool_result для экономии контекста Claude
✅ Агентный цикл с поддержкой до 10 итераций

## 📦 Зависимости

### Основные

- `anthropic>=0.40.0` - Anthropic SDK для Claude
- `clickhouse-connect>=0.7.0` - клиент ClickHouse
- `pandas>=2.0.0` - анализ данных
- `numpy>=1.24.0` - численные вычисления
- `pyarrow>=14.0.0` - Parquet формат
- `matplotlib>=3.7.0` - графики
- `seaborn>=0.12.0` - красивые графики

### Опциональные (для API сервера)

- `fastapi>=0.104.0` - веб-фреймворк
- `uvicorn[standard]>=0.24.0` - ASGI сервер
- `pydantic>=2.0.0` - валидация данных

### Утилиты

- `python-dotenv>=1.0.0` - загрузка .env
- `tabulate>=0.9.0` - красивые таблицы

## 🎓 Обучение и использование

### Для новичков

1. Прочитайте [QUICKSTART.md](QUICKSTART.md)
2. Запустите `test_agent.py`
3. Попробуйте примеры запросов
4. Экспериментируйте!

### Для разработчиков

1. Изучите [FULL_IMPLEMENTATION_SPEC.md](FULL_IMPLEMENTATION_SPEC.md)
2. Посмотрите код `composite_agent.py`
3. Поймите механизм tool_use
4. Адаптируйте под свои нужды

### Для деплоя на продакшн

1. Прочитайте [INSTALLATION.md](INSTALLATION.md)
2. Настройте systemd сервис
3. Настройте nginx
4. Настройте мониторинг и логи

## 🚧 Будущие улучшения

### В ближайшее время
- [ ] Веб-интерфейс для общения с агентом
- [ ] Docker контейнер для легкого деплоя
- [ ] Расширенное логирование

### В перспективе
- [ ] Поддержка множественных баз данных
- [ ] Интеграция с ML моделями
- [ ] Экспорт отчетов в PDF
- [ ] Планировщик задач (cron-like)

## 📞 Поддержка

При возникновении проблем:
1. Проверьте документацию в `INSTALLATION.md`
2. Посмотрите примеры в `QUICKSTART.md`
3. Изучите логи в консоли
4. Создайте Issue на GitHub

## 🏆 Итог

Создан полнофункциональный агент, готовый к использованию:

✅ Работает с ClickHouse
✅ Анализирует данные на Python
✅ Строит графики
✅ Сохраняет историю
✅ Легко устанавливается
✅ Хорошо документирован
✅ Готов к интеграции с фронтенд

**Следующий шаг:** Создать веб-интерфейс для удобного общения с агентом.
