# 🎉 Решение готово: API сервер и веб-интерфейс

## ✅ Что было создано

Для развертывания вашего ClickHouse Analysis Agent API на домене **server.asktab.ru** (Beget) были созданы все необходимые компоненты:

---

## 📚 1. Документация (3 файла)

### [SETUP_SUMMARY.md](SETUP_SUMMARY.md) - Главный файл
**Начните отсюда!** Содержит всю информацию в одном месте:
- Что было создано
- Как запустить
- Примеры использования
- Интеграция с front-end
- FAQ и troubleshooting

### [QUICKSTART_DEPLOYMENT.md](QUICKSTART_DEPLOYMENT.md) - Быстрый старт
Пошаговая инструкция на 5 минут:
1. SSH подключение
2. Клонирование и установка
3. Настройка .env
4. Настройка systemd
5. Настройка nginx
6. SSL сертификат
7. Готово!

### [DEPLOYMENT.md](DEPLOYMENT.md) - Полное руководство
Детальная документация со всеми нюансами:
- Подготовка сервера
- Настройка ClickHouse
- Настройка автозапуска
- Мониторинг и логи
- Безопасность
- Масштабирование
- Резервное копирование

---

## 🌐 2. Веб-интерфейс

### [static/index.html](static/index.html)
Готовый веб-интерфейс для тестирования API:

**Функции:**
- ✅ Красивый адаптивный дизайн (работает на телефоне и компьютере)
- ✅ Интерактивный чат с агентом
- ✅ Отображение графиков (base64 PNG)
- ✅ Примеры запросов (кнопки)
- ✅ Индикатор статуса API (онлайн/оффлайн)
- ✅ Управление сессиями
- ✅ Отображение использованных tools

**Доступ после развертывания:**
```
https://server.asktab.ru
```

---

## ⚙️ 3. Конфигурационные файлы

### [agent-api.service](agent-api.service)
Systemd служба для автозапуска API сервера:
- Автоматический запуск при загрузке системы
- Автоматический перезапуск при падении
- Логирование в systemd journal
- Загрузка переменных из .env

**Установка:**
```bash
sudo cp agent-api.service /etc/systemd/system/
sudo systemctl enable agent-api.service
sudo systemctl start agent-api.service
```

### [nginx.conf](nginx.conf)
Конфигурация nginx для проксирования:
- Проксирование API запросов на порт 8000
- Раздача статического веб-интерфейса
- Поддержка SSL (HTTPS)
- Увеличенные таймауты для долгих запросов
- Логирование

**Установка:**
```bash
sudo cp nginx.conf /etc/nginx/sites-available/agent-api
sudo ln -s /etc/nginx/sites-available/agent-api /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

---

## 🔧 4. Обновленный API сервер

### [api_server.py](api_server.py)
Обновлен для работы с веб-интерфейсом:
- ✅ Добавлена раздача статических файлов
- ✅ CORS настроен для работы с любым front-end
- ✅ Endpoints:
  - `GET /health` - health check
  - `GET /api/status` - статус API
  - `GET /api/info` - информация о сервисе
  - `POST /api/analyze` - основной endpoint для анализа
  - `GET /api/chat-stats` - статистика
  - `GET /` - веб-интерфейс (статика)

---

## 🚀 Как запустить (кратко)

### 1. Подключитесь к серверу
```bash
ssh username@server.asktab.ru
```

### 2. Установите проект
```bash
cd /opt
sudo mkdir -p agent_api
cd agent_api
git clone https://github.com/vdubrovin1704-ops/agent_clickhouse_python_v1.git
cd agent_clickhouse_python_v1
./setup.sh
```

### 3. Настройте .env
```bash
nano .env
```
Заполните:
- `ANTHROPIC_API_KEY` - ваш API ключ
- `CLICKHOUSE_HOST` - хост базы данных
- `CLICKHOUSE_USER` - пользователь
- `CLICKHOUSE_PASSWORD` - пароль
- `CLICKHOUSE_DATABASE` - база данных

### 4. Настройте службы
```bash
# Systemd
sudo cp agent-api.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable agent-api.service
sudo systemctl start agent-api.service

# Nginx
sudo cp nginx.conf /etc/nginx/sites-available/agent-api
sudo ln -s /etc/nginx/sites-available/agent-api /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx

# SSL
sudo certbot --nginx -d server.asktab.ru
```

### 5. Проверьте работу
```bash
# Через curl
curl https://server.asktab.ru/health

# Через браузер
# Откройте https://server.asktab.ru
```

---

## 💻 Интеграция с front-end

Ваш front-end сайт может обращаться к API так:

```javascript
// Отправка запроса
const response = await fetch('https://server.asktab.ru/api/analyze', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
    query: 'Покажи топ-10 товаров по выручке',
    session_id: 'optional-session-id'
  })
});

const result = await response.json();

if (result.success) {
  console.log('Ответ:', result.text_output);

  // Графики в base64
  result.plots.forEach(plot => {
    const img = document.createElement('img');
    img.src = `data:image/png;base64,${plot}`;
    document.body.appendChild(img);
  });
}
```

**CORS уже настроен**, front-end может обращаться с любого домена!

---

## 📊 Примеры запросов для тестирования

После развертывания попробуйте через веб-интерфейс:

### Простые запросы:
```
Покажи список всех таблиц в базе данных
```

### С анализом:
```
Покажи топ-10 товаров по выручке за последний месяц
```

### С графиками:
```
Построй bar chart топ-10 товаров по выручке
```

### Сложные:
```
Проанализируй данные о продажах за 2024 год, покажи статистику
и построй 3 графика: выручка по месяцам, топ товаров,
распределение по категориям
```

---

## 🗄️ База данных ClickHouse

### Если база уже есть:
Просто укажите параметры подключения в `.env`

### Если нужно создать:
Инструкции в [DEPLOYMENT.md](DEPLOYMENT.md), раздел "Настройка базы данных ClickHouse"

Варианты:
1. **Яндекс Cloud** - управляемый кластер (рекомендуется)
2. **Локальная установка** - на сервере
3. **Другой хостинг** - любой ClickHouse

---

## ✅ Что будет работать после развертывания

### 1. API Endpoints
- `https://server.asktab.ru/health` - проверка здоровья
- `https://server.asktab.ru/api/info` - информация
- `https://server.asktab.ru/api/analyze` - анализ данных

### 2. Веб-интерфейс
- `https://server.asktab.ru` - красивый UI для тестирования

### 3. Автозапуск
- ✅ API запускается при загрузке сервера
- ✅ Автоматически перезапускается при падении
- ✅ Работает 24/7

### 4. Мониторинг
```bash
# Логи API
sudo journalctl -u agent-api.service -f

# Логи nginx
sudo tail -f /var/log/nginx/agent-api-error.log

# Статус
sudo systemctl status agent-api.service
```

---

## 🎯 Итоговый чеклист

- [ ] SSH доступ к server.asktab.ru
- [ ] Проект склонирован в `/opt/agent_api/agent_clickhouse_python_v1`
- [ ] Зависимости установлены (`./setup.sh`)
- [ ] `.env` настроен с API ключами
- [ ] ClickHouse база данных доступна
- [ ] `test_agent.py` работает
- [ ] systemd служба запущена
- [ ] nginx настроен
- [ ] SSL сертификат установлен
- [ ] `https://server.asktab.ru` открывается
- [ ] API отвечает на запросы
- [ ] Front-end может обращаться к API

---

## 📞 Если что-то не работает

### Логи API:
```bash
sudo journalctl -u agent-api.service -n 100
```

### Перезапуск:
```bash
sudo systemctl restart agent-api.service
sudo systemctl restart nginx
```

### Проверка:
```bash
# API работает?
curl http://localhost:8000/health

# nginx работает?
sudo nginx -t
sudo systemctl status nginx
```

---

## 📚 Документация

1. **[SETUP_SUMMARY.md](SETUP_SUMMARY.md)** - Полная информация и примеры
2. **[QUICKSTART_DEPLOYMENT.md](QUICKSTART_DEPLOYMENT.md)** - Быстрый старт за 5 минут
3. **[DEPLOYMENT.md](DEPLOYMENT.md)** - Детальное руководство
4. **[README.md](README.md)** - О проекте
5. **[INSTALLATION.md](INSTALLATION.md)** - Локальная разработка

---

## 🎉 Готово!

Теперь у вас есть:
- ✅ Полная документация по развертыванию
- ✅ Веб-интерфейс для тестирования
- ✅ API готовый для integration с front-end
- ✅ Автозапуск и мониторинг
- ✅ SSL/HTTPS
- ✅ Работа 24/7

**Следуйте инструкциям в [QUICKSTART_DEPLOYMENT.md](QUICKSTART_DEPLOYMENT.md) для быстрого развертывания!**

---

**Успешного запуска!** 🚀
