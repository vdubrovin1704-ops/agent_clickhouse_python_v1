# ClickHouse AI Agent

Интеллектуальный CLI-агент для работы с базой данных ClickHouse через AI (Claude). Агент умеет выполнять SQL запросы, экспортировать данные в формат Parquet и анализировать их с помощью Python.

## Возможности

- 🔍 **Изучение структуры БД** - автоматическое получение списка таблиц и колонок
- 💾 **Выполнение SQL запросов** - выполнение SELECT запросов к ClickHouse
- 📊 **Экспорт в Parquet** - автоматическое сохранение результатов в Parquet формате
- 🐍 **Python анализ** - выполнение Python кода для анализа данных
- 🤖 **AI-ассистент** - интеллектуальное взаимодействие с базой данных через Claude
- 🔒 **Безопасность** - SSL подключение к Яндекс Cloud ClickHouse

## Быстрый старт

### 1. Установка зависимостей

```bash
cd cli_agent
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### 2. Настройка окружения

```bash
cp .env.example .env
# Отредактируйте .env файл, добавьте ваш ANTHROPIC_API_KEY
```

### 3. Скачивание SSL сертификата

```bash
wget https://storage.yandexcloud.net/cloud-certs/CA.pem -O cli_agent/YandexInternalRootCA.crt
```

### 4. Запуск

```bash
python3 cli_agent.py
```

## Подробная инструкция

Полная инструкция по установке и настройке на Ubuntu доступна в файле [INSTALLATION.md](INSTALLATION.md).

## Примеры использования

### Просмотр таблиц в базе данных

```
❓ Ваш запрос: покажи все таблицы в базе
```

### Выгрузка данных

```
❓ Ваш запрос: выгрузи первые 100 строк из таблицы visits_complete
```

Агент выполнит запрос, сохранит результат в Parquet и покажет первые 5 строк.

### Анализ данных

```
❓ Ваш запрос: сколько записей в таблице visits_complete за последний месяц?
```

Агент:
1. Составит SQL запрос с фильтрацией по дате
2. Выгрузит данные
3. Выведет результат

## Архитектура

Проект состоит из одного основного файла `cli_agent.py`, который включает:

- **ClickHouseClient** - класс для работы с ClickHouse
- **execute_python_code** - функция для безопасного выполнения Python кода
- **run_agent** - агентный цикл с использованием Claude API
- **TOOLS** - определения инструментов для Claude (list_tables, clickhouse_query, python_analysis)

### Процесс работы

```
Запрос пользователя
     ↓
Claude анализирует запрос
     ↓
Вызов инструмента list_tables (структура БД)
     ↓
Вызов инструмента clickhouse_query (SQL запрос)
     ↓
Сохранение в Parquet
     ↓
Вызов инструмента python_analysis (анализ данных)
     ↓
Вывод результата
```

## Технологии

- **Python 3.11+**
- **Anthropic SDK** - для работы с Claude AI
- **clickhouse-connect** - клиент для ClickHouse
- **pandas** - анализ данных
- **pyarrow** - работа с Parquet форматом
- **python-dotenv** - управление конфигурацией

## Требования

- Python 3.11 или выше
- API ключ Anthropic (Claude)
- Доступ к ClickHouse базе данных
- Ubuntu 20.04+ (или другая Linux система)

## Конфигурация

Все настройки хранятся в файле `.env`:

```bash
ANTHROPIC_API_KEY=your-api-key
CLICKHOUSE_HOST=your-clickhouse-host.mdb.yandexcloud.net
CLICKHOUSE_PORT=8443
CLICKHOUSE_USER=your-user
CLICKHOUSE_PASSWORD=your-password
CLICKHOUSE_DATABASE=your-database
CLICKHOUSE_SSL_CERT_PATH=YandexInternalRootCA.crt
```

## Структура проекта

```
.
├── cli_agent/
│   ├── cli_agent.py           # Основной файл агента
│   ├── requirements.txt        # Зависимости Python
│   ├── .env.example           # Пример конфигурации
│   ├── .env                   # Конфигурация (не в Git)
│   ├── YandexInternalRootCA.crt  # SSL сертификат
│   └── temp_data/             # Временные Parquet файлы
├── INSTALLATION.md            # Подробная инструкция по установке
├── CLI_AGENT_SPEC.md          # Техническая спецификация
└── README.md                  # Этот файл
```

## Безопасность

⚠️ **ВАЖНО:**
- Никогда не загружайте файл `.env` в Git
- Храните API ключи в безопасности
- Используйте SSL соединение с ClickHouse
- Агент выполняет только SELECT запросы (защита от модификации данных)

## Ограничения

- Поддерживаются только SELECT запросы
- Автоматический LIMIT 50000 строк (если не указан явно)
- Максимум 10 итераций на один запрос

## Устранение неполадок

См. раздел "Устранение неполадок" в [INSTALLATION.md](INSTALLATION.md).

## Лицензия

MIT License

## Автор

Создано для работы с ClickHouse в Яндекс Cloud.
