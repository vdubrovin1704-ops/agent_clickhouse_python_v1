server {
    listen 80;
    server_name your-server.example.com;

    # Редирект HTTP → HTTPS (раскомментировать после настройки SSL)
    # return 301 https://$host$request_uri;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

        # Важно: агент может работать долго (несколько итераций с ClickHouse)
        proxy_read_timeout 300s;
        proxy_connect_timeout 60s;
        proxy_send_timeout 300s;
    }
}

# Блок HTTPS (раскомментировать после получения SSL сертификата через certbot)
# server {
#     listen 443 ssl;
#     server_name your-server.example.com;
#
#     ssl_certificate /etc/letsencrypt/live/your-server.example.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/your-server.example.com/privkey.pem;
#
#     location / {
#         proxy_pass http://127.0.0.1:8000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_read_timeout 300s;
#         proxy_connect_timeout 60s;
#         proxy_send_timeout 300s;
#     }
# }
