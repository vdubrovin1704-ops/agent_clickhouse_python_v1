# Anthropic API (напрямую, БЕЗ OpenRouter)
ANTHROPIC_API_KEY=sk-ant-...

# ClickHouse (Яндекс Cloud)
CLICKHOUSE_HOST=your-host.mdb.yandexcloud.net
CLICKHOUSE_PORT=8443
CLICKHOUSE_USER=your_user
CLICKHOUSE_PASSWORD=your_password
CLICKHOUSE_DATABASE=your_database

# SSL сертификат Яндекс Cloud (опционально — скачивается скриптом setup.sh)
# Если файл не существует — подключение без верификации (verify=False)
CLICKHOUSE_SSL_CERT_PATH=YandexInternalRootCA.crt

# URL сервера (для будущего веб-интерфейса)
SERVER_URL=https://your-server.example.com
